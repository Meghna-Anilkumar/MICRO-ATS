import type { Candidate } from "../types";

const statuses = ["Applied", "Technical Round", "Offered"];
export function CandidatePipeline({ candidates, onStatusChange }: { candidates: Candidate[]; onStatusChange: (candidate: Candidate, status: string) => void }) {
  return <section className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200 lg:col-span-2"><p className="text-sm font-semibold uppercase tracking-widest text-blue-600">Pipeline</p><h2 className="mt-1 text-xl font-bold text-slate-950">Candidates</h2><div className="mt-5 space-y-3">{candidates.length === 0 ? <p className="rounded-2xl bg-slate-50 p-4 text-sm text-slate-500">No candidates yet.</p> : candidates.map((candidate) => <article key={candidate._id} className="rounded-2xl border border-slate-200 p-4"><p className="font-bold text-slate-900">{candidate.name}</p><p className="mt-1 truncate text-sm text-slate-500">{candidate.email}</p><select aria-label={`Status for ${candidate.name}`} value={candidate.status} onChange={(event) => onStatusChange(candidate, event.target.value)} className="mt-3 w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm font-semibold text-slate-700 outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-100">{statuses.map((status) => <option key={status}>{status}</option>)}</select></article>)}</div></section>;
}
