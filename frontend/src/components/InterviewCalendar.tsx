import type { Candidate, InterviewSlot } from "../types";
type Entry = InterviewSlot & { candidate: Candidate };
type Props = { entries: Entry[]; hasInterviewer: boolean; timeZone: string };
const days = Array.from({ length: 7 }, (_, index) => { const day = new Date(); day.setHours(0, 0, 0, 0); day.setDate(day.getDate() + index); return day; });
const isSameDay = (value: string, day: Date) => { const date = new Date(value); return date.getFullYear() === day.getFullYear() && date.getMonth() === day.getMonth() && date.getDate() === day.getDate(); };
export function InterviewCalendar({ entries, hasInterviewer, timeZone }: Props) { return <section className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200 lg:col-span-3"><div className="flex flex-wrap items-baseline justify-between gap-2"><div><p className="text-sm font-semibold uppercase tracking-widest text-violet-600">Calendar</p><h2 className="mt-1 text-xl font-bold text-slate-950">Interviewer schedule</h2><p className="mt-1 text-sm text-slate-500">Times shown in your browser’s local timezone.</p></div><span className="rounded-full bg-violet-50 px-3 py-1 text-xs font-medium text-violet-700">{timeZone}</span></div>{!hasInterviewer ? <p className="mt-6 rounded-2xl bg-slate-50 p-4 text-sm text-slate-500">Select an interviewer to view their schedule.</p> : <div className="mt-6 overflow-x-auto"><div className="grid min-w-[700px] grid-cols-7 gap-2">{days.map((day) => { const dayEntries = entries.filter((entry) => isSameDay(entry.timeBlock.start, day)); return <div key={day.toISOString()} className="min-h-56 rounded-2xl border border-slate-200 bg-slate-50 p-2"><div className="border-b border-slate-200 pb-2 text-center"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">{day.toLocaleDateString(undefined, { weekday: "short" })}</p><p className="text-lg font-bold text-slate-900">{day.getDate()}</p></div><div className="mt-2 space-y-2">{dayEntries.map((entry) => <article key={`${entry.candidate._id}-${entry.timeBlock.start}`} className="rounded-xl bg-violet-600 p-2 text-xs text-white shadow-sm"><p className="font-bold">{entry.candidate.name}</p><p className="mt-1 text-violet-100">{new Date(entry.timeBlock.start).toLocaleTimeString([], { hour: "numeric", minute: "2-digit" })} – {new Date(entry.timeBlock.end).toLocaleTimeString([], { hour: "numeric", minute: "2-digit" })}</p></article>)}{dayEntries.length === 0 && <p className="pt-3 text-center text-xs text-slate-400">Available</p>}</div></div>; })}</div>{entries.length === 0 && <p className="mt-4 text-sm text-slate-500">No interviews booked in the next seven days.</p>}</div>}</section>; }
