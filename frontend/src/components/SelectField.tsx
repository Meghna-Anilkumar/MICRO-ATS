type Option = { value: string; label: string };
type Props = { label: string; value: string; placeholder: string; options: Option[]; onChange: (value: string) => void; onAdd: () => void; error?: string };
export function SelectField({ label, value, placeholder, options, onChange, onAdd, error }: Props) { return <label className="block text-sm font-semibold text-slate-700">{label}<div className="mt-1.5 flex gap-2"><div className="relative min-w-0 flex-1"><select required value={value} onChange={(event) => onChange(event.target.value)} className={`w-full appearance-none rounded-xl border bg-gradient-to-r from-slate-900 to-slate-800 px-3 py-3 pr-9 text-sm font-medium text-white shadow-sm outline-none transition hover:from-slate-800 hover:to-slate-700 focus:ring-4 ${error ? "border-rose-400 focus:ring-rose-100" : "border-slate-800 focus:ring-violet-200"}`}><option value="" className="bg-white text-slate-700">{placeholder}</option>{options.map((option) => <option key={option.value} value={option.value} className="bg-white text-slate-800">{option.label}</option>)}</select><span className="pointer-events-none absolute inset-y-0 right-3 flex items-center text-violet-200">⌄</span></div><button type="button" onClick={onAdd} className="rounded-xl bg-black px-3 text-sm font-bold text-white transition hover:bg-slate-800" aria-label={`Add ${label.toLowerCase()}`}>+</button></div>{error && <span className="mt-1 block text-xs font-medium text-rose-600">{error}</span>}</label>; }
